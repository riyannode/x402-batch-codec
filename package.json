{
  "name": "x402-batch-codec",
  "version": "0.2.0",
  "description": "Portable unsigned evidence codec for Circle Gateway submitBatch batch mappings and Arc address participation.",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build:js && npm run build:types",
    "build:js": "tsup src/index.ts --format esm,cjs --clean",
    "build:types": "tsc -p tsconfig.build.json --emitDeclarationOnly",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "prepare": "npm run build",
    "prepublishOnly": "npm run typecheck && npm test && npm run build"
  },
  "keywords": [
    "x402",
    "circle",
    "gateway",
    "arc",
    "batch",
    "codec",
    "stablecoin"
  ],
  "license": "MIT",
  "dependencies": {
    "viem": "^2.55.8"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsup": "^8.4.0",
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/riyannode/x402-batch-codec.git"
  }
}
